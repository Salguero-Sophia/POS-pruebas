export const getUrlAps = async () => {

    try {
        
        // const { host, apsPort } = await getConfig();

        // return `http://${host}:${apsPort}/ExternalAPI/PayAtTable`;

        

    } catch (error: any) {

        return "";

    }

}