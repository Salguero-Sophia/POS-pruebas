export interface Store {
    id: string;
    host: string;
    apiPort: string;
    apsPort: string;
    name: string;
    phone: string;
    email: string;
    logo: string;
    address: string;
    code: string;
    isActive: boolean;
    createdAt: Date;
    updatedAt: Date;
}
